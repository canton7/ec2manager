<UserControl x:Class="Ec2Manager.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:supp="clr-namespace:Ec2Manager.Support"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200" Padding="10">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" FontWeight="Bold" FontSize="25" HorizontalAlignment="Center" Margin="0,0,0,20">Ec2Manager</TextBlock>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Version, StringFormat='Version: {0}'}" Margin="0,5"/>
        <TextBlock DockPanel.Dock="Top" Margin="0,5">Created by Antony Male</TextBlock>
        <TextBlock DockPanel.Dock="Top" Margin="0,5"><Hyperlink supp:HyperlinkUtilities.Action="ShowHomepage"><TextBlock Text="{Binding HomepageUrl}"/></Hyperlink></TextBlock>
        <Button x:Name="Close" DockPanel.Dock="Bottom" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0"  Style="{StaticResource buttonStyle}">Close</Button>
    </DockPanel>
</UserControl>
