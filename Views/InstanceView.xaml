<UserControl x:Class="Ec2Manager.Views.InstanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <DockPanel>
        <GroupBox Header="Instance Properties" DockPanel.Dock="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding InstanceName, StringFormat='Instance Name: {0}'}"/>
                <TextBlock Grid.Row="0" Grid.Column="1">Etc etc...</TextBlock>
                <Button Grid.Row="1" Grid.Column="2" HorizontalAlignment="Right">Terminate Instance</Button>
            </Grid>
        </GroupBox>
        
        
        
        <TabControl Name="Items" DockPanel.Dock="Top" VerticalAlignment="Stretch"/>
    </DockPanel>
</UserControl>
