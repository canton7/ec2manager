<Window x:Class="Ec2Manager.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:s="http://github.com/canton7/Stylet"
        mc:Ignorable="d" 
        Padding="10"
        ResizeMode="NoResize"
        d:DesignHeight="300" Width="224">
    <DockPanel>
        <DockPanel.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="0,5"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="0,0,0,10"/>
            </Style>
        </DockPanel.Resources>
        
        <TextBlock DockPanel.Dock="Top">AWS Access Key:</TextBlock>
        <TextBox x:Name="MainConfig_AwsAccessKey" DockPanel.Dock="Top" Width="200" HorizontalAlignment="Left"/>
        <TextBlock DockPanel.Dock="Top"> AWS Secret Key:</TextBlock>
        <TextBox x:Name="MainConfig_AwsSecretKey" DockPanel.Dock="Top" Width="200" HorizontalAlignment="Left"/>
        <TextBlock DockPanel.Dock="Top" Margin="0">Forgotten your credentials?</TextBlock>
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10"><Hyperlink Command="{s:Action ShowCredentials}">Find them here</Hyperlink></TextBlock>
        
        <Grid DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="75"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="Save" Grid.Column="0" IsDefault="True">Save</Button>
            <Button x:Name="Cancel" Grid.Column="2" IsCancel="True">Cancel</Button>
        </Grid>
    </DockPanel>
</Window>
